package DFS

import (
	"github.com/AxelUser/algo_workshop/Graphs/graph"
)

// DepthFirstSearch performs traversing
func DepthFirstSearch(g *graph.AdjacencyListGraph, s int) *map[int]int {
	pred := make(map[int]int)
	visited := make(map[int]bool)

	return &pred
}

func dfsVisit(g *graph.AdjacencyListGraph, n *graph.Node) {

}
